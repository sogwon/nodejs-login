# ----- 서버 / MySQL -----
# PORT=3000

# MySQL 연결 
# DB_HOST=호스트주소
# DB_PORT=3306
# DB_USER=사용자명
# DB_PASSWORD=비밀번호
# DB_NAME=데이터베이스이름

# ----- Auth API -----
# JWT Access Token (필수, 운영 시 반드시 변경)
# JWT_SECRET=your-jwt-secret-at-least-32-chars
# JWT_ISSUER=auth.example.com
# Access 만료(초, 기본 900=15분), Refresh 만료(초, 기본 2592000=30일)
# ACCESS_TOKEN_EXPIRES_IN=900
# REFRESH_TOKEN_EXPIRES_IN=2592000
# Provider 목록의 authUrl에 사용 (리다이렉트 기준 URL)
# AUTH_BASE_URL=https://auth.example.com
# BASE_URL=https://api.example.com
# OTP 테스트용: 1로 설정 시 otp/send 응답에 debugCode 포함 (예제 앱에서 코드 자동 입력, 개발 전용)
# OTP_DEBUG=1
